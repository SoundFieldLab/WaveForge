# 改动内容记录（2026-08-18）

- 提交者：universe233666-alt
- 合并基线：远程 master `1f95de0f6`（提交前已整合远程最新改动）
- 说明：本地工作（立体模型歌词模式 + 分析器接线）在远程最新代码之上合并提交；`src/App.tsx`、`HANDOVER.md` 与远程改动重叠的部分已通过三路合并解决，无冲突残留。

## ✨ 新增功能

### Folia Diorama 立体模型歌词模式（`src/components/foliaDiorama/`，12 个文件）

- 基于 folia 开源项目语义的立体模型歌词呈现：镜头 / 走廊 / 排版结构
- HDR bloom 后期管线（three `EffectComposer` + `UnrealBloomPass` + `OutputPass`，**无新依赖**）
- Fresnel 玻璃着色器、封面主色派生配色、几何面数提升
- fBM 分形星云纹理 + StarShell 3D 星壳 + 银河带（模块级缓存跨曲目复用）
- 相机 roll 35% 转移、镜头选镜权重重平衡、滞后 lerp 视差跟随
- 详细设计见 `HANDOVER.md` §6（2026-08-16 多维歌词质感升级条目）

## 🔧 修改

| 文件 | 变更 | 说明 |
|---|---|---|
| `src/App.tsx` | +2 | 歌词组件接入 `pulseStore` / `analyzerStore`（与远程最新版三路合并） |
| `src/hooks/useAudioAnalyzer.ts` | +26/-1 | 分析器能力增强 |
| `src/components/MultidimensionalLyrics.tsx` | +69/-289 | Diorama 歌词模式质感升级（对照 folia 重做呈现层） |
| `src/components/GloriousLyrics.tsx` | +34/-21 | 歌词展示配套调整 |
| `src/components/WallpaperLyrics.tsx` | +26/-15 | 歌词展示配套调整 |
| `HANDOVER.md` | — | 项目状态更新 + Diorama 质感升级记录 + 端口占用坑（ReWaveForge 抢占 3001/3101）（冲突已解决） |

## 📝 说明

- `package-lock.json` 本地被 `npm install` 自动更新，未纳入本次提交
- 本地遗留的 28 个根目录旧文档、cuefield 死代码、旧版 Python wheel 等未纳入提交
- 提交前已 `npm install` 补装远程新增依赖；Electron 二进制已通过 npmmirror 镜像重装
