# pre-push 钩子必须保持 LF：Windows autocrlf 检出 CRLF 会让 `#!/bin/sh` 变成
# `#!/bin/sh\r`，git push 时报 cannot spawn。只做定向规则，不加全局 * 规则以免触发全库行尾重整。
.githooks/* text eol=lf

# Windows 批处理必须 CRLF：cmd.exe 解析 LF 换行的 .bat 时会把行首串成错误 token
# （表现为「'xxx' 不是内部或外部命令」且伴随中文乱码），脚本无法运行。
*.bat text eol=crlf
